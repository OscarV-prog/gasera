[build]
  base = "."
  command = "pnpm --filter @acme/nextjs build"
  publish = "apps/nextjs/.next"

[build.environment]
  NODE_VERSION = "22"
  PNPM_FLAGS = "--no-frozen-lockfile"

[[plugins]]
  package = "@netlify/plugin-nextjs"
